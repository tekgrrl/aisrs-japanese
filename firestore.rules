rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: TEMPORARY DIAGNOSTIC RULE
    // This allows all reads and writes from the client.
    // This is ONLY safe because we are on the local emulator.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}